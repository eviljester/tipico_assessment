README
The selenium webdriver I wrote this with is compatible with Chrome version 85, otherwise it'll fail out.
Additionally, I did not have time to complete the last step so it only has a <Console.Writeline("Temp");> in its place.

